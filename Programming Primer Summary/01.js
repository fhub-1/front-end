var profile = "joseph";
var twitterhandle = "josehub121";
var age = 24;

function whoAmI(myName, myNickName,age) {
    // takes in something and return it back
    console.log(`
    hi, I am ${myName} (aka ${myNickName}),
    and I am ${age} years old
    `);
}

whoAmI(profile, twitterhandle, age)